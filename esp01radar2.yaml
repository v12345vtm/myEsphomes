esphome:
  name: esp01radar2
  friendly_name: esp01Radar2

esp8266:
  board: esp01_1m


substitutions:
  device_name: esp01radar2     

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: "SSY/bC0tjiGYhHWVvXwMqMGftu67v4P8RUyebwJDwHQ="

ota:
  password: "8385fb99198273ec31e309f99a7f3946"

wifi:
  networks:
  - ssid: !secret wifi_ssid
    password: !secret wifi_password
  - ssid: !secret wifi_ssid2
    password: !secret wifi_password   

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Esp01Radar2 Fallback Hotspot"
    password: "3GCi8w5wd58V"

captive_portal:



web_server:
  port : 80


text_sensor:
  - platform: wifi_info
    ip_address:
      name: "${device_name}_IP" 
    mac_address:
      name: "${device_name}_mac"
    ssid:
      name: "${device_name}_usedssid" 


#  Led
output:
  - platform: gpio
    pin: GPIO2
    id: outp
    inverted: yes
    

dallas:
  - pin: GPIO0
    update_interval: 5s 

##  0x7b0114548fc8ff28



light:
  - platform: binary
    output: outp
    name: esp-out-GPIO2



 


# Example configuration entry with 2 sensors and filter
#sensor:
sensor:
  - platform: wifi_signal # Reports the WiFi signal strength/RSSI in dB
    name: "${device_name}_WiFi Signal dB"
    id: wifi_signal_db
    update_interval: 80s
    entity_category: "diagnostic"
    disabled_by_default: true
    internal: true
  - platform: copy # Reports the WiFi signal strength in %
    source_id: wifi_signal_db
    name: "${device_name}_WiFi_Pct"
    filters:
      - lambda: return min(max(2 * (x + 100.0), 0.0), 100.0);
    unit_of_measurement: "Signal %"
    entity_category: "diagnostic"
  - platform: uptime
    name: "${device_name}_Uptime"   
 




 

# Individual sensors
#sensor:
  
  - platform: dallas
    address: 0x7b0114548fc8ff28 #zie log tijdens het opstarten wat je adres is vb 0x3ac7f0a308646128
    name: "Radar2Temperature"  
    id: radar2temp
    filters :
    - calibrate_linear:
      - 0.0 -> 0.0
      - 19 ->17       


button:
#Restart esp
  - platform: restart
    name: "${device_name}_restart"   



   
    